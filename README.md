## Starting the Paymentop Test Project

1. Clone repo `git clone https://github.com/AndriiTsybrukh/paymentop-test-project.git`;
2. Run `npm i` to install dependencies;
3. Run `ng s` to serve the app.
